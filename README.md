# Minesweeper in C
A simple minesweeper game written in C using Raylib

## Building the game from source
 - `git clone https://github.com/elfoteo/minesweeper_c.git`
 - `git submodule update --init --recursive`
 - `make`

## Running the game
 - `build/main`
